result
result-*
.direnv/

# agenix encrypted secrets will be tracked, but never plaintext
secrets/*.age.key

# Track Claude Code skills (overrides global .claude/ ignore)
!.claude/
.claude/*
!.claude/skills/
!.claude/settings.local.json
